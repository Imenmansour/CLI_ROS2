installation dds implementations gurumnetworks gurumdds edit on github gurumnetworks gurumdds ï rmw_gurumdds is a implementation of the ros middleware interface using gurumnetworks gurumdds. more information  gurumdds is available on our website: https://gurum.cc/index_eng prerequisites ï the following description assumes that you have completed the âenvironment setupâ process from the installing ros 2 via deb packages or from the building ros 2 on ubuntu linux . rmw_gurumdds requires version of gurumdds-2.8.x. deb packages of gurumdds are provided in the ros 2 apt repositories on ubuntu. windows binary installer of gurumdds will be supported soon. gurumdds requires a license. see the next page: https://gurum.cc/free_trial_eng.html after requesting a trial license,  download the license from the license homepage. after getting a license, move it to the following location. dds version license location <= 2.7.2860 /etc/flame >= 2.7.2861 /etc/gurumnet 2.8.x /etc/gurumnet install packages ï the easiest way is to install from ros 2 apt repository. when ros-jazzy-rmw-gurumdds-cpp is installed, gurumdds-2.8 is also installed. sudo apt install ros-jazzy-rmw-gurumdds-cpp build from source code ï building from source code is also another way to install. first, clone rmw_gurumdds in the ros 2 workspace source directory. cd ros2_ws/src git clone https://github.com/ros2/rmw_gurumdds -b jazzy ros2/rmw_gurumdds then, install necessary packages for gurumdds. cd .. rosdep install --from src -i --rosdistro jazzy finally, run colcon build. colcon build --symlink-install switch to rmw_gurumdds ï switch from other rmw to rmw_gurumdds by specifying the environment variable. export rmw_implementation = rmw_gurumdds_cpp see also: working with multiple rmw implementations run the talker and listener ï now run talker and listener to test gurumdds. donât forget to set up environment by setup script. ros2 run demo_nodes_cpp talker ros2 run demo_nodes_cpp listener other versions v: jazzy releases jazzy (latest) iron humble galactic (eol) foxy (eol) eloquent (eol) dashing (eol) crystal (eol) in development rolling